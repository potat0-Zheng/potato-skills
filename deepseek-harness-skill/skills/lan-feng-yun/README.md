# 揽风云 技能维护日志

## 用途

围绕指定主题进行多源信息搜索、整理和汇总，产出结构化信息汇编报告或事件时间线。

## 维护记录

- **2026-06-10 (v4)** — 集成知乎 API 管道：信源获取新增 L0 层（知乎专用），检测 zhihu.com 链接自动切换至 crawl.py（走 API 绕过 L1 阻断）；API 返回的 img URL 天然无推荐流污染，可直送 collect_images.py；阶段 2 增加知乎分支判断。无需额外依赖——复用知乎 skill 已有 cookie 配置
- **2026-06-10 (v3)** — 图片管线重构：新增 `collect_images.py`（合并文章HTML抓取→图片URL提取→Referer自动重试→编码→排序为一次调用，替代此前 AI 手动 5 步试错链）；新增 `report_template.html`；SKILL.md HTML 导出章节精简
- **2026-06-10 (v2)** — 全面精简 SKILL.md（340→155 行）：信源获取失败三层策略合并为单表，图像处理融入输出规范，删除使用示例；新增 `embed_image.py`（下载→编码，Pillow 优先/urllib 回退）和 `fetch_page.py`（正文抓取绕过 L1）；HTML 导出支持 1–3 张图像 base64 内联嵌入
- **2026-06-10** — 新增「4.3 图像资料处理」章节，建立无多模态能力下的图像信源轻量处理方案（仅整理出处/描述，不做视觉分析）；新增 L1 安全验证层阻断应对策略（Claude Code WebFetch 域名拦截的三步应对链）；编写 `fetch_page.py` 辅助抓取脚本；输出规范中新增「图像资料附录」可选章节
- **2026-05-15** — 初始化技能框架；新增「信源可达性：被墙外媒的应对策略」章节，建立 4 步优先策略链（存档站替代 → 摘要先行 → 多源三角确认 → RSS/API 绕过），禁止使用外媒中文版替代英文原站
